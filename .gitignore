# Runtime state only — anchored to repo root so shared/data/ (game content) is NOT ignored
/data/
!/data/.gitkeep
node_modules/
*.log
.DS_Store
.env

# Raw asset source packs (100k+ files) — the game ships only what tools copy into client/assets
/model assets/
/Animated Chests/
